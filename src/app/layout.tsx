import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Navigation } from "@/components/Navigation";
import { Footer } from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Industrial Roofing & Gutters | Professional Construction Services",
  description: "Expert industrial roofing and gutter installation, repair, and maintenance services. Licensed, insured, and experienced in commercial construction projects.",
  keywords: "industrial roofing, commercial gutters, roof repair, construction services, waterproofing, metal roofing",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen flex flex-col">
          <Navigation />
          <main className="flex-grow">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  );
}